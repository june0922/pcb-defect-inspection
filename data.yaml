# YOLO 데이터셋 정의 — DeepPCB 6-class
# processed 경로는 config.yaml → get_paths() 가 결정하므로
# 여기서는 플레이스홀더를 두고, train.py 에서 동적으로 덮어씌움.
# (ultralytics는 절대 경로를 받으므로 런타임에 주입)

# TODO: train.py 에서 paths['processed'] 를 읽어 아래 경로를 채울 것
path: PLACEHOLDER_SET_BY_TRAIN   # project_root/data/processed
train: images/train
val:   images/val
test:  images/test

nc: 6
names:
  - open       # 0
  - short      # 1
  - mousebite  # 2
  - spur       # 3
  - copper     # 4
  - pinhole    # 5
